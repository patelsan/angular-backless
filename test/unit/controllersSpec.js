'use strict';

/* jasmine specs for controllers go here */
/*
describe('controllers', function(){
    beforeEach(module('myApp'));
    beforeEach(module('myApp.controllers'));


  it('should ....', inject(function() {
    //spec body
  }));

  it('UserController', inject(function(_$httpBackend_, $rootScope, $controller) {
    var scope, ctrl, mockBackend;
      mockBackend = _$httpBackend_;

      mockBackend.expectGET('http://localhost/listitapp/api/login', function(headers){
        return headers['X-Token'] === '--auth-token--';
      }).respond({Status: 'Succeeded' });

      scope = $rootScope.$new();
      ctrl = $controller('UserController', {$scope: scope});

      mockBackend.flush();

      expect(scope.loginStatus).toEqual('Succeeded');
  }));
});
*/